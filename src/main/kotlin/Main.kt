package com.abitofhelp.simpleshadow

class Main

/**
 * Function main simply writes a hello to the terminal.  The real test
 * is running the shadowJar in the libs folder from the command-line.
 * There should be no errors and the hello should appear.
 */
fun main() {
    println("Howdy!")
}